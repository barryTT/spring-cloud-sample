spring:
  application:
    name: configServer

  rabbitmq:
    host: localhost

  cloud:
    inetutils:
      ignored-interfaces:
        - enp0s3
        - enp0s8
#        - enp0s9
        - docker0


