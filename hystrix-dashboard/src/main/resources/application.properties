spring.application.name=hystrixdashboard
spring.cloud.config.uri=${vcap.services.${PREFIX:}configserver.credentials.uri:http://user:password@localhost:8888}

info.component=Hystrix Dashboard

endpoints.restart.enabled=true
endpoints.shutdown.enabled=true

server.port=7979

logging.level.ROOT=INFO
logging.level.org.springframework.web: DEBUG

#Region where eureka is deployed -For AWS specify one of the AWS regions, for other datacenters specify a arbitrary string
#indicating the region.This is normally specified as a -D option (eg) -Deureka.region=us-east-1
eureka.client.region=default

#For eureka clients running in eureka server, it needs to connect to servers in other zones
eureka.client.preferSameZone=false

#Change this if you want to use a DNS based lookup for determining other eureka servers. For example
#of specifying the DNS entries, check the eureka-client-test.properties, eureka-client-prod.properties
#shouldUseDns: false

eureka.client.us-east-1.availabilityZones=default

#Virtual host name by which the clients identifies this service
eureka.instance.virtualHostName: ${spring.application.name}